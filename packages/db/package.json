{
  "name": "@beta/db",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules",
    "db": "sst shell drizzle-kit",
    "db:studio": "sst shell drizzle-kit studio"
  },
  "dependencies": {
    "drizzle-orm": "^0.31.1",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "drizzle-kit": "^0.22.2"
  }
}
